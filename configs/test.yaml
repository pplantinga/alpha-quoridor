board_size: 3
walls_per_player: 1

model:
  hidden_dim: 16
  num_blocks: 1

training:
  num_self_play_games: 1
  batch_size: 2
  lr: 0.001
  num_iterations: 2
  buffer_size: 100

mcts:
  num_simulations: 5
  c_puct: 1.5
  temperature: 1.0
  dirichlet_noise_alpha: 0.3
  dirichlet_noise_epsilon: 0.25
