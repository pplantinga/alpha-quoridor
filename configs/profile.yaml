board_size: 5
walls_per_player: 3

model:
  hidden_dim: 32
  num_blocks: 1

training:
  num_self_play_games: 1
  batch_size: 2
  lr: 0.001
  num_iterations: 1
  buffer_size: 100

mcts:
  num_simulations: 50
  c_puct: 1.5
  temperature: 1.0
  dirichlet_noise_alpha: 0.3
  dirichlet_noise_epsilon: 0.25
